version: '2'
services:
  server:
    build: ./server
    ports:
      - "7777:7777"
  prometheus:
    image: prom/prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
